This is the csv simulation of the bernstein vazirani algorithm.

How to use: 
1. Enter the function input in the 1_function_design.csv file.
Save the file.
2. Run the 2_Bernstein_Vazirani.py thats in the same folder for simulation. Its just the same structure as the original DJ algorithm.

For Actual IBM Implementation, run the IBM version of the same file after entering your IBM Credentials.

For both the above files, the number of shots is as the input.

make sure that you are in that particular directory where these files reside.

Output: The input(encoded) string.